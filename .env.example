# Copy this file to .env and paste your key (get one at https://console.typesafe.ai/keys).
# .env is ignored by git, so the key never gets committed. `jevmeter setup` is the easier option.
TYPESAFE_API_KEY=
